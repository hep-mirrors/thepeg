// -*- C++ -*-
//
// This is the implementation of the inlined member functions of
// the TwoCutBase class.
//

namespace ThePEG {

inline TwoCutBase::TwoCutBase() {}

inline TwoCutBase::TwoCutBase(const TwoCutBase & x)
  : Interfaced(x) {}

inline void TwoCutBase::doupdate() throw(UpdateException) {
  Interfaced::doupdate();
  // First update base class.
  bool redo = touched();
  // redo if touched.
//  UpdateChecker::check(aDependentMember, redo);
  // Update referenced objects on which this depends redo is set to true
  // if the dependent object is touched.
//  for_each(ContainerOfDependencies, UpdateChecker(redo));
  // Update a container of references.
//  for_each(MapOfDependencies, UpdateMapChecker(redo));
  // Update a map of references.
  if ( !redo ) return;
  // return if nothing has been touched. Otherwise do the actual update.
//  touch()
  // Touch if anything has changed.
}

inline void TwoCutBase::doinit() throw(InitException) {
  Interfaced::doinit();
}

inline void TwoCutBase::dofinish() {
  Interfaced::dofinish();
}

inline void TwoCutBase::doinitrun() {
  Interfaced::doinitrun();
}

inline void TwoCutBase::rebind(const TranslationMap & trans)
  throw(RebindException) {
  // dummy = trans.translate(dummy);
  Interfaced::rebind(trans);
}

inline IVector TwoCutBase::getReferences() {
  IVector ret = Interfaced::getReferences();
  // ret.push_back(dummy);
  return ret;
}

}
